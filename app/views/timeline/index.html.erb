  <!-- <div style="height: 300px; display: inline;"> -->
  <form>
    <fieldset>
        <input class="datepicker filter" name="custom_date" type="date">
        <!-- <input type="hidden" name="custom_date_hidden" value> -->
        <!-- <button type="button" class="filterbutton">Filter Date</button> -->
    </fieldset>
  </form>
   <form id="resourcefilter">  
      <fieldset>
        <label for="datefilter">
          <input type="checkbox" class="filter" id="Post" value="Blogs" />
          <span>Blogs</span>
        </label>
        <label for="secondRegularCheckbox">
          <input type="checkbox" class="filter" id="Tweet" value="Twitter" />
          <span>Twitter</span>
        </label>
        <label for="secondRegularCheckbox">
          <input type="checkbox" class="filter" id="GhEvent" value="Github" />
          <span>Github</span>
        </label>
      </fieldset>
      <button type="submit">Filter Events</button>
    </form>
  <form id="peoplefilter">
    <fieldset>
      <% Person.all.each do |person|%>
        <%= content_tag :input, :type => "checkbox", :class => "filter", :id =>"#{person.id}", :value => "#{person.name}" do-%>
        <span><%= "#{person.name}" %></span>
        <% end %>
      <% end %>
    </fieldset>
    <button type="submit">Filter People</button>
  </form>
    
<!-- </div> -->


<nav>
    <ol id="filter-nav">
      <li id="topnav">Filters</li>
      <li class="subnav" id="all">All</li>
      <li class="subnav" id="blog">Blog</li>
      <li class="subnav" id="tweet">Tweet</li>
      <li class="subnav" id="github">Github</li>
      <li class="subnav" id="yesterday">Yesterday</li>
      <li class="subnav" id="month">Last Month</li>
    </ol>
  </nav>
<!-- <div class="container"> -->
  <div id="teamline">
<!--     <div id="date-header"><h2><%= Date.today.strftime("%B %d, %Y") %></h2></div>
 -->    <div id="timeline-top"> </div>
    <div id="timeline">
      <ol class="timeline" id="spine">
        <%= render 'event' %>
      </ol>
    </div>
  </div>

<%= will_paginate @grouped_events %>

<script type="text/javascript">
    $(function(){
      $( '[type=date], .datepicker' ).pickadate({
          date_min: [ 2012, 10, 02 ],
          date_max: true,
          format_submit: 'yyyy-mm-dd'
           
      })  
    });



</script>
