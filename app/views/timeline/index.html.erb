
<% @events.each do |date, events| %>
  <%= time_ago_in_words(date) %> ago
  <ul>
  <% events.each do |event| %>
    <%= content_tag :li,  :data => {:date => "#{event.created_at}"} do -%>
      <%= raw(event.content) %> 
    <% end -%>
  <% end %>
  </ul>
<% end %>
